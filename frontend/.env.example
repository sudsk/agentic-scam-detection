# frontend/.env - FIXED FOR EXTERNAL IP
# Frontend Environment Configuration for External Server

# FIXED: Use external IP instead of localhost
REACT_APP_API_URL=<ip>:8000
REACT_APP_WS_URL=<ip>:8000

# Environment settings
REACT_APP_ENVIRONMENT=development
REACT_APP_VERSION=2.0.0

# REMOVED: proxy configuration (will use REACT_APP_API_URL instead)
# No need for "proxy": "http://localhost:8000" in package.json
